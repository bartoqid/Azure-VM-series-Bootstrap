# Azure-VM-series-Bootstrap
This is a terraform template to bootstrap VM-series in Azure

run

"terraform plan" 

then

"terraform apply -auto-approve"

to terminate everything

"terraform destroy -auto-approve"
